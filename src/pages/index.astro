---
import Base from "../layouts/Base.astro";
import { projects } from "../data/projects";
---
<Base title="Paul Harris · Portfolio" description="Software engineer building fast, clean UIs and APIs.">
  <section class="hero">
    <h1>Hi, I’m Paul 👋</h1>
    <p class="lead">
      I build lean, reliable web apps. Here are a few highlights — code, demos, and what I used.
    </p>
    <a class="cta" href="https://github.com/pdharris100">View GitHub</a>
  </section>

  <section id="projects">
    <h2>Projects</h2>
    <div class="grid">
      {projects.map(p => (
        <article class="card">
          <h3>{p.title}</h3>
          <p>{p.desc}</p>
          <p class="meta">{p.stack}</p>
          <p>
            <a href={p.repo}>Code</a>
            {p.demo && <> · <a href={p.demo}>Live</a></>}
          </p>
        </article>
      ))}
    </div>
  </section>
</Base>
